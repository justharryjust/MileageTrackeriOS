Load the orchestrator agent prompt from .claude/agents/orchestrator.md. Check the GitHub Projects board for items that need attention. Compare against the stored state in .claude/project-state.json. For each trigger condition detected, spawn the appropriate sub-agent (Scoping, Developer, or QA) using the Agent tool with the prompt from the corresponding .claude/agents/ file. After all sub-agents complete, report what was done and ask if you should check again.
