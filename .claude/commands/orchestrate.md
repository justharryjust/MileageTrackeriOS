Load the orchestrator agent prompt from .claude/agents/orchestrator.md. Check the board, dispatch sub-agents for every item needing work (Backlog→Scoping, Refined/Ready→Developer, In Review→QA), and call ScheduleWakeup to self-schedule the next check in ~2.5 minutes. Do NOT ask for confirmation. Do NOT wait for user input. This is fully autonomous — keep the loop running.
