Load the Product Tester agent prompt from .claude/agents/product-tester.md. First read README.md for the canonical build instructions. Then boot the iPhone 17 simulator, build the app using the exact xcodebuild command from the README, install it, and run through the full test plan: onboarding, home screen, trip recording, trip detail, reports, settings, and edge cases. Use Mobile MCP tools to interact with the app, take screenshots, and verify behavior. Produce a structured report with Bugs Found and Improvement Suggestions when done. Save screenshots to a temp directory for reference.
